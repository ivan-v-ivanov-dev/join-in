server:
  port: 8081
  max-http-header-size: 10MB

#Mongo
spring:
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: storage
      host: localhost
      port: 27017

