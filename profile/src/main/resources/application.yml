server:
  port: 8081

#Mongo
spring:
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: storage
      host: localhost
      port: 27017

